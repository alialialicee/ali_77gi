# Loading ritual assets

The current loading ritual uses CSS-only fallback shapes so the app builds without image files.

Future replacements can be added here as `loading-mudang.png` / `loading-mudang.webp` and `jakdu-track.png` / `jakdu-track.webp`, then wired into `LoadingRitual` without changing the loading flow.
